#ifndef GRUVBOX_DARK_H_
#define GRUVBOX_DARK_H_
// Cores padrão (normal)
static char normfgcolor[]                = "#458588";  // Foreground color (texto)
static char normbgcolor[]                = "#282828";  // Background color (fundo)
static char normbordercolor[]            = "#3c3836";  // Border color (bordas)
static char normfloatcolor[]             = "#458588";  // Float color (janelas flutuantes)

// Cores selecionadas (selecionado)
static char selfgcolor[]                 = "#fbf1c7";  // Foreground color (texto)
static char selbgcolor[]                 = "#689d6a";  // Background color (fundo)
static char selbordercolor[]             = "#689d6a";  // Border color (bordas)
static char selfloatcolor[]              = "#689d6a";  // Float color (janelas flutuantes)

// Cores do título normal
static char titlenormfgcolor[]           = "#ebdbb2";  // Foreground color (texto)
static char titlenormbgcolor[]           = "#282828";  // Background color (fundo)
static char titlenormbordercolor[]       = "#3c3836";  // Border color (bordas)
static char titlenormfloatcolor[]        = "#458588";  // Float color (janelas flutuantes)

// Cores do título selecionado
static char titleselfgcolor[]            = "#fbf1c7";  // Foreground color (texto)
static char titleselbgcolor[]            = "#282828";  // Background color (fundo)
static char titleselbordercolor[]        = "#689d6a";  // Border color (bordas)
static char titleselfloatcolor[]         = "#689d6a";  // Float color (janelas flutuantes)

// Cores das tags normal
static char tagsnormfgcolor[]            = "#ebdbb2";  // Foreground color (texto)
static char tagsnormbgcolor[]            = "#282828";  // Background color (fundo)
static char tagsnormbordercolor[]        = "#3c3836";  // Border color (bordas)
static char tagsnormfloatcolor[]         = "#458588";  // Float color (janelas flutuantes)

// Cores das tags selecionadas
static char tagsselfgcolor[]             = "#fbf1c7";  // Foreground color (texto)
static char tagsselbgcolor[]             = "#3c3836";  // Background color (fundo)
static char tagsselbordercolor[]         = "#689d6a";  // Border color (bordas)
static char tagsselfloatcolor[]          = "#689d6a";  // Float color (janelas flutuantes)

// Cores ocultas
static char hidnormfgcolor[]             = "#928374";  // Foreground color (texto)
static char hidselfgcolor[]              = "#a89984";  // Foreground color (texto selecionado)
static char hidnormbgcolor[]             = "#282828";  // Background color (fundo)
static char hidselbgcolor[]              = "#282828";  // Background color (fundo selecionado)

// Cores de urgência
static char urgfgcolor[]                 = "#ebdbb2";  // Foreground color (texto)
static char urgbgcolor[]                 = "#282828";  // Background color (fundo)
static char urgbordercolor[]             = "#cc241d";  // Border color (bordas)
static char urgfloatcolor[]              = "#458588";  // Float color (janelas flutuantes)

static char c000000[]                    = "#000000"; // Placeholder value

static char ltsymbolbgcolor[]            = "#282828";
static char ltsymbolfgcolor[]            = "#689d6a";

static char tagsunusedfgcolor[]          = "#928374"; // Cor do texto para tags não utilizadas
static char tagsunusedbgcolor[]          = "#282828"; // Cor de fundo para tags não utilizadas
static char tagsunusedbordercolor[]      = "#3c3836"; // Cor da borda para tags não utilizadas
static char tagsunusedfloatcolor[]       = "#458588"; // Cor de flutuação para tags não utilizadas

#endif // GRUVBOX_DARK_H_
